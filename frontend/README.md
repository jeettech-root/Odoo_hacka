# VendorBridge Frontend

This is a minimal React + Vite frontend scaffold for the VendorBridge backend.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The dev server runs on `http://localhost:5173` by default. API calls point to `http://localhost:3000`.
